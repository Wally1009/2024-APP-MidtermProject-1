import React from 'react';
import IHiddenProps from './types';
export declare const Hidden: ({ children, from, till, only, colorMode: colorModeforHide, platform: platformforHide, }: IHiddenProps) => React.JSX.Element;
